/* The map function */

let points = [10, 20, 30];

// let addOne = function (element) {
//     return element + 1;
// };

// let addOne = (element) => {
//     return element + 1;
// };

// points = points.map(addOne);

points = points.map(e => e + 2 );
console.log(points);